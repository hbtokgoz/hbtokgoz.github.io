name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Aynı anda tek dağıtım; ama süren bir dağıtımı ASLA iptal etme — yarıda
# kesilen Pages dağıtımı siteyi eski sürümde bırakıyor.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - uses: actions/deploy-pages@v4
        id: deployment
        with:
          # Varsayılan 10 dk yetmiyor: dağıtım "deployment_queued" durumunda
          # daha uzun bekleyebiliyor ve zaman aşımı onu iptal ediyor.
          timeout: 1800000
